<% if current_user.role != "admin" %>
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h3 class="text-center deny-header"><i class="fas fa-ban text-danger"></i> You do not have access to this content <i class="fas fa-lock text-danger"></i></h3>
      </div>
    </div>
  <div> <!-- Container end -->
<% else %>
  <div class="container-fluid">
    <div class="row"> <!-- Links to new and edit -->
      <div class="col-12">
        <p class="text-center deny-header"> <%= link_to "Create a new game", new_game_path %></p>
        <p class="text-center deny-header"> <%= link_to "my games", new_game_path %></p>
      </div>
    </div>

    <div class="row"> <!-- Statistics -->
      <div class="col-12 col-md-6">

      </div>
    </div>
  </div>
<% end %>
